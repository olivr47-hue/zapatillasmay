diff --git a/frontend/tienda/index.html b/frontend/tienda/index.html
index 2aab09c..431880a 100644
--- a/frontend/tienda/index.html
+++ b/frontend/tienda/index.html
@@ -259,31 +259,29 @@
       font-family: var(--font-mono);
     }
 
+    /* HERO CONFIG VARIABLES */
+    :root {
+      --visor-360-alto-pc: 100%;
+      --visor-360-alto-movil: 100%;
+      --visor-360-escala-zapato: 0.92; /* Altura del zapato relativa al hero (los frames ya vienen recortados/centrados) */
+    }
+
     /* HERO */
     .hero {
-  margin-top: calc(36px + 72px);
-  height: calc(100vh - 108px);
-  min-height: calc(100vh - 108px);
-  background:
-    radial-gradient(ellipse 80% 60% at 20% 30%,  rgba(200,150,122,0.18) 0%, transparent 55%),
-    radial-gradient(ellipse 60% 50% at 80% 70%,  rgba(181,104,122,0.12) 0%, transparent 55%),
-    radial-gradient(ellipse 50% 40% at 55% 20%,  rgba(232,208,185,0.20) 0%, transparent 50%),
-    linear-gradient(145deg, #f7ede4 0%, #f2e4d8 35%, #ecdbd0 60%, #f5ece4 100%);
-  display: flex;
-  align-items: center;
-  justify-content: center;
-  position: relative;
-  overflow: hidden;
-}
+      margin-top: calc(36px + 72px);
+      height: calc(100vh - 108px);
+      min-height: calc(100vh - 108px);
+      background: #ffffff !important; /* Todo el lienzo de fondo es blanco puro */
+      display: flex;
+      align-items: center;
+      justify-content: center;
+      position: relative;
+      overflow: hidden;
+    }
 
     .hero::before {
-  content: '';
-  position: absolute;
-  inset: 0;
-  background:
-    radial-gradient(ellipse at 30% 50%, rgba(200,150,122,0.12) 0%, transparent 60%),
-    radial-gradient(ellipse at 70% 80%, rgba(200,150,122,0.07) 0%, transparent 50%);
-}
+      display: none !important; /* Desactivamos cualquier fondo crema en el hero */
+    }
 
 /* Líneas decorativas tipo editorial fashion */
 .hero::after {
@@ -297,10 +295,26 @@
 }
 
     .hero-content {
-      text-align: center;
+      max-width: 1400px;
+      width: 100%;
+      margin: 0 auto;
+      padding: 0 40px;
       position: relative;
-      z-index: 1;
-      padding: 40px;
+      z-index: 2; /* Encima de la zapatilla */
+      text-align: left;
+      display: flex;
+      flex-direction: column;
+      align-items: flex-start;
+      justify-content: center;
+      height: 100%;
+      pointer-events: none; /* Permite arrastrar el zapato de fondo */
+    }
+
+    .hero-content > p,
+    .hero-content > h1,
+    .hero-content > div.hero-cta {
+      max-width: 55%;
+      pointer-events: auto; /* Los botones y textos sí son clickeables */
     }
 
     .hero-eyebrow {
@@ -314,12 +328,12 @@
 
     .hero-title {
       font-family: var(--font-display);
-      font-size: clamp(3rem, 8vw, 7rem);
+      font-size: clamp(2.5rem, 5vw, 4.8rem);
       font-weight: 300;
-      color: var(--white);
-      line-height: 1;
-      letter-spacing: -1px;
-      margin-bottom: 12px;
+      color: var(--black);
+      line-height: 1.05;
+      letter-spacing: -0.5px;
+      margin-bottom: 16px;
     }
 
     .hero-title em {
@@ -329,71 +343,187 @@
     }
 
     .hero-subtitle {
-      font-size: 1rem;
-      color: rgba(255,255,255,0.5);
-      margin-bottom: 40px;
+      font-size: clamp(0.95rem, 1.2vw, 1.15rem);
+      color: var(--gray-600);
+      margin-bottom: 24px;
       font-weight: 300;
+      line-height: 1.5;
     }
 
     .hero-cta {
-      display: inline-flex;
+      display: flex;
       gap: 16px;
       flex-wrap: wrap;
-      justify-content: center;
     }
 
     .btn-primary {
-  display: inline-flex;
-  align-items: center;
-  gap: 8px;
-  padding: 14px 36px;
-  background: linear-gradient(135deg, #C8967A 0%, #b5687a 100%);
-  color: white;
-  font-family: var(--font-body);
-  font-size: 0.82rem;
-  font-weight: 600;
-  text-transform: uppercase;
-  letter-spacing: 1.5px;
-  border: none;
-  border-radius: 100px;
-  cursor: pointer;
-  text-decoration: none;
-  transition: var(--transition);
-  box-shadow: 0 4px 16px rgba(200,150,122,0.35);
-}
+      display: inline-flex;
+      align-items: center;
+      gap: 8px;
+      padding: 14px 36px;
+      background: linear-gradient(135deg, #C8967A 0%, #b5687a 100%);
+      color: white;
+      font-family: var(--font-body);
+      font-size: 0.82rem;
+      font-weight: 600;
+      text-transform: uppercase;
+      letter-spacing: 1.5px;
+      border: none;
+      border-radius: 100px;
+      cursor: pointer;
+      text-decoration: none;
+      transition: var(--transition);
+      box-shadow: 0 4px 16px rgba(200,150,122,0.35);
+    }
 
-.btn-primary:hover {
-  background: linear-gradient(135deg, #d4a48a 0%, #c27888 100%);
-  box-shadow: 0 8px 28px rgba(200,150,122,0.5);
-  transform: translateY(-2px);
-}
-.btn-primary:active { transform: scale(0.97); }
+    .btn-primary:hover {
+      background: linear-gradient(135deg, #d4a48a 0%, #c27888 100%);
+      box-shadow: 0 8px 28px rgba(200,150,122,0.5);
+      transform: translateY(-2px);
+    }
+    .btn-primary:active { transform: scale(0.97); }
 
     .btn-outline {
-  display: inline-flex;
-  align-items: center;
-  gap: 8px;
-  padding: 13px 36px;
-  background: transparent;
-  color: white;
-  font-family: var(--font-body);
-  font-size: 0.82rem;
-  font-weight: 600;
-  text-transform: uppercase;
-  letter-spacing: 1.5px;
-  border: 1.5px solid rgba(255,255,255,0.7);
-  border-radius: 100px;
-  cursor: pointer;
-  text-decoration: none;
-  transition: var(--transition);
-  backdrop-filter: blur(4px);
-}
+      display: inline-flex;
+      align-items: center;
+      gap: 8px;
+      padding: 13px 36px;
+      background: transparent;
+      color: var(--black);
+      font-family: var(--font-body);
+      font-size: 0.82rem;
+      font-weight: 600;
+      text-transform: uppercase;
+      letter-spacing: 1.5px;
+      border: 1.5px solid var(--black);
+      border-radius: 100px;
+      cursor: pointer;
+      text-decoration: none;
+      transition: var(--transition);
+      backdrop-filter: blur(4px);
+    }
 
-.btn-outline:hover {
-  border-color: white;
-  background: rgba(255,255,255,0.12);
-  color: white;
-}
+    .btn-outline:hover {
+      border-color: var(--pink);
+      background: var(--pink-light);
+      color: var(--pink);
+    }
+
+    /* Ocultar el fondo de boutique que no combina */
+    .hero-bg {
+      display: none !important;
+    }
+
+    /* Zapato y Visor en PC (Fondo Blanco - Mitad Derecha Completa del Viewport) */
+    #hero-shoe-scroll {
+      position: absolute;
+      top: 0; bottom: 0; right: 0; left: 46%; /* Confinada a la mitad derecha: no invade el texto */
+      width: auto !important;
+      height: 100% !important;
+      pointer-events: auto;
+      z-index: 1; /* Detrás de las letras */
+      display: flex;
+      align-items: center;
+      justify-content: center;
+      padding-right: 4%;
+      cursor: grab;
+      touch-action: pan-y; /* Permite scroll vertical; el arrastre horizontal gira */
+      opacity: 1 !important; /* Evita que .hero-content > * lo oculte */
+      background: #ffffff !important; /* Todo el fondo de la sección es blanco */
+      overflow: hidden; /* Recorta los excesos de la imagen */
+      user-select: none;
+      -webkit-user-select: none;
+      -moz-user-select: none;
+      -ms-user-select: none;
+    }
+
+    #hero-shoe-img {
+      max-height: calc(100% * var(--visor-360-escala-zapato)) !important;
+      max-width: 96%;
+      width: auto;
+      height: auto;
+      object-fit: contain;
+      user-select: none;
+      -webkit-user-drag: none;
+      -webkit-user-select: none;
+      transition: transform 0.35s cubic-bezier(0.23, 1, 0.32, 1);
+    }
+
+    /* Clase para estado ampliado */
+    #hero-shoe-img.zoomed {
+      transform: scale(1.35) translateY(5px);
+      cursor: zoom-out;
+    }
+
+    /* Botón flotante de zoom */
+    .btn-zoom {
+      position: absolute;
+      top: 16px;
+      right: 16px;
+      width: 38px;
+      height: 38px;
+      border-radius: 50%;
+      background: #FBF7F3;
+      border: 1px solid rgba(200, 150, 122, 0.2);
+      color: #2A1A0E;
+      font-size: 1.1rem;
+      display: flex;
+      align-items: center;
+      justify-content: center;
+      cursor: pointer;
+      z-index: 10;
+      transition: all 0.2s ease;
+      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
+    }
+    .btn-zoom:hover {
+      background: #C8967A;
+      color: white;
+      transform: scale(1.06);
+    }
+    .btn-zoom:active {
+      transform: scale(0.96);
+    }
+
+    #hero-shoe-scroll:active { cursor: grabbing; }
+
+    .viewer-360-badge {
+      position: absolute;
+      bottom: 12px;
+      left: 50%;
+      transform: translateX(-50%);
+      background: rgba(42, 26, 14, 0.05);
+      color: var(--gray-600);
+      padding: 5px 14px;
+      border-radius: 100px;
+      font-size: 0.72rem;
+      font-weight: 500;
+      pointer-events: none;
+      border: 1px solid rgba(42, 26, 14, 0.08);
+      white-space: nowrap;
+      box-shadow: 0 2px 8px rgba(0,0,0,0.02);
+      z-index: 4;
+    }
+
+    /* ── Subrayado animado "León, GTO" ── */
+    .hero-title .leon-gto {
+      position: relative;
+      display: inline;
+    }
+    .hero-title .leon-gto::after {
+      content: '';
+      position: absolute;
+      bottom: -5px;
+      left: 0;
+      width: 100%;
+      height: 2px;
+      background: #a56780;
+      transform: scaleX(0);
+      transform-origin: left center;
+      transition: transform 0.9s cubic-bezier(0.23, 1, 0.32, 1);
+      border-radius: 1px;
+      opacity: 0.8;
+    }
+    .hero-title .leon-gto.line-drawn::after { transform: scaleX(1); }
 
     /* ── CRO: TRUST STRIP ── */
     .cro-trust-strip {
@@ -1697,8 +1827,11 @@
   z-index: 0;
   filter: contrast(1.08) brightness(0.97) saturate(1.05);
   opacity: 0;
-  transform: scale(1.1) translateY(30px);
-  transition: opacity 0.5s ease;
+  transition: opacity 1.4s ease;
+}
+
+.hero.loaded .hero-bg {
+  opacity: 1;
 }
 
 @media (min-width: 768px) {
@@ -1821,13 +1954,7 @@
   50%       { transform: translateX(-50%) scaleX(0.75); opacity: 0.4; }
 }
 
-@media (max-width: 768px) {
-  .hero-shoe-wrap {
-    right: -5%;
-    width: 65%;
-    opacity: 0.5;
-  }
-}
+/* Mobile shoe override removed in favor of unified responsive layouts */
 
 /* Reducir movimiento */
 @media (prefers-reduced-motion: reduce) {
@@ -1847,7 +1974,10 @@
 .hero::before {
   content: '';
   position: absolute;
-  inset: 0;
+  left: 0;
+  top: 0;
+  bottom: 0;
+  width: 50%;
   z-index: 1;
   background: linear-gradient(90deg, rgba(250,248,246,0.50) 0%, transparent 55%),
               radial-gradient(ellipse at 30% 50%, rgba(200,150,122,0.10) 0%, transparent 60%),
@@ -1856,36 +1986,198 @@
 }
 
 @media (max-width: 768px) {
+  /* ── HERO MÓVIL: zapato arriba, texto abajo, foto de fondo ── */
+
+  /* 1. Foto de fondo visible */
   .hero-bg {
-    background-position: 60% top;
-    background-size: cover;
-    background-repeat: no-repeat;
-    top: 0;
-    height: 100%;
+    display: block !important;
+    top: 0 !important;
+    height: 100% !important;
+    background-position: center 18% !important;
   }
-  .hero-cta {
+  .hero.loaded .hero-bg {
+    opacity: 1 !important;
+  }
+
+  /* 2. Ocultar lupa */
+  #btn-zoom {
+    display: none !important;
+  }
+
+  /* 3. Menos frases */
+  .hero-eyebrow  { display: none !important; }
+  .hero-subtitle { display: none !important; }
+
+  /* 4. Contenedor hero */
+  .hero {
+    margin-top: calc(36px + 56px + 52px) !important;
+    height: auto !important;
+    min-height: calc(100vh - 144px) !important;
+    padding: 0 !important;
+    background: #FBF7F3 !important;
+    display: flex;
     flex-direction: column !important;
-    align-items: center !important;
-    width: 100%;
+    align-items: center;
+    justify-content: flex-start !important;
+  }
+
+  /* 5. Scrim: transparente arriba (zapato), crema abajo (texto) */
+  .hero::before {
+    display: block !important;
+    width: 100% !important;
+    background: linear-gradient(
+      to top,
+      rgba(251,247,243,0.97) 0%,
+      rgba(251,247,243,0.80) 26%,
+      rgba(251,247,243,0.25) 52%,
+      rgba(251,247,243,0.00) 100%
+    ) !important;
+    z-index: 1 !important;
+  }
+
+  /* 6. Sin línea decorativa vertical en móvil */
+  .hero::after {
+    display: none !important;
+  }
+
+  /* Ocultar partículas en móvil */
+  .hero-particles { display: none !important; }
+
+  /* 7. Visor 360 — ARRIBA (order 1), fondo blanco que cubre la foto */
+  #hero-shoe-scroll {
+    position: relative !important;
+    inset: auto !important;
+    left: auto !important;
+    right: auto !important;
+    order: 1 !important;
+    width: 100% !important;
+    max-width: 100% !important;
+    height: 55vh !important;
+    min-height: 300px !important;
+    margin: 0 !important;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    background: #ffffff !important;
+    border: none !important;
+    pointer-events: auto !important;
+    z-index: 2 !important;
+    padding: 0 !important;
+    cursor: grab;
+    touch-action: pan-y;
+  }
+
+  /* Badge "360°" en esquina superior derecha */
+  #hero-shoe-scroll::after {
+    content: '360°';
+    position: absolute;
+    top: 14px;
+    right: 14px;
+    font-family: var(--font-sans);
+    font-size: 0.62rem;
+    font-weight: 600;
+    letter-spacing: 0.06em;
+    color: rgba(165,103,128,0.85);
+    background: rgba(255,255,255,0.92);
+    border: 1px solid rgba(165,103,128,0.22);
+    padding: 3px 8px;
+    border-radius: 20px;
+    pointer-events: none;
+    z-index: 3;
   }
-  .hero-cta .btn-primary,
-  .hero-cta .btn-outline {
-    width: 80% !important;
-    justify-content: center !important;
+
+  /* Ocultar el badge largo del visor */
+  .viewer-360-badge { display: none !important; }
+
+  /* 8. Texto — ABAJO (order 2) */
+  .hero-content {
+    max-width: 100% !important;
+    padding: 0 24px 28px !important;
+    text-align: center !important;
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    justify-content: center;
+    position: relative !important;
+    z-index: 2 !important;
+    order: 2 !important;
+    pointer-events: auto !important;
   }
+
+  .hero-content > p,
+  .hero-content > h1,
+  .hero-content > div.hero-cta {
+    max-width: 100% !important;
+    pointer-events: auto !important;
+  }
+
+  /* 9. Título más elegante + sin subrayado en León, Gto */
   .hero-title {
-    text-shadow: 0 2px 30px rgba(245,240,232,0.4), 0 0 60px rgba(245,240,232,0.2);
+    font-size: 2.5rem !important;
+    line-height: 1.07 !important;
+    letter-spacing: -0.02em !important;
+    color: var(--black) !important;
+    text-shadow: none !important;
+    margin-bottom: 12px !important;
   }
   .hero-title em {
-    text-shadow: 0 2px 20px rgba(184,124,130,0.5), 0 0 40px rgba(184,124,130,0.3);
+    text-shadow: none !important;
   }
-  .hero-subtitle {
-    text-shadow: 0 1px 10px rgba(30,20,21,0.3);
+  .hero-title .leon-gto::after {
+    display: none !important;
+  }
+
+  /* 10. Botones pequeños */
+  .hero-cta {
+    flex-direction: row !important;
+    justify-content: center !important;
+    gap: 10px !important;
+    width: 100% !important;
+    margin-top: 12px !important;
+  }
+  .hero-cta .btn-primary,
+  .hero-cta .btn-outline {
+    width: auto !important;
+    flex: 1;
+    max-width: 150px;
+    padding: 10px 14px !important;
+    font-size: 0.72rem !important;
+    justify-content: center !important;
   }
+  .btn-outline {
+    border-color: var(--black) !important;
+    color: var(--black) !important;
+  }
+  .btn-outline:hover {
+    border-color: var(--pink) !important;
+    background: var(--pink-light) !important;
+    color: var(--pink) !important;
+  }
+
   header { top: 36px; }
   .header-inner { padding: 0 20px; height: 56px; }
   .logo img { height: 120px !important; max-width: 240px; object-fit: contain; padding: 8px 0; }
-  .hero { margin-top: calc(36px + 56px + 52px); }
+
+  .hero-shoe-wrap {
+    width: 100% !important;
+    height: 100% !important;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    opacity: 1 !important;
+    transform: none !important;
+  }
+
+  .hero-shoe-wrap img {
+    max-height: 80% !important; /* Tamaño gigante responsivo en móvil */
+    width: auto !important;
+    object-fit: contain !important;
+  }
+
+  .viewer-360-badge {
+    bottom: 0px !important;
+  }
+
   .nav-cats { display: none; }
   .header-search { display: none; }
 
@@ -2726,25 +3018,23 @@
     <span class="hp hp8"></span>
   </div>
 
-  <!-- Zapatilla 3D — wrapper scroll + wrapper levitate separados -->
-  <div id="hero-shoe-scroll" style="position:absolute;right:5%;bottom:0;width:48%;max-width:640px;height:100%;pointer-events:none;z-index:3;">
+  <!-- Zapatilla 3D / Visor 360 (Ahora fuera de hero-content, alineado al 50% derecho del viewport) -->
+  <div id="hero-shoe-scroll">
+    <button class="btn-zoom" id="btn-zoom" title="Acercar detalles" aria-label="Acercar detalles">🔍</button>
     <div class="hero-shoe-wrap" id="hero-shoe-wrap" aria-hidden="true">
       <img id="hero-shoe-img"
-        src="https://res.cloudinary.com/dybdtehhs/image/upload/w_900,f_auto,q_auto/v1/zapatillasmay/hero-zapato"
-        srcset="https://res.cloudinary.com/dybdtehhs/image/upload/w_480,f_auto,q_auto/v1/zapatillasmay/hero-zapato 480w, https://res.cloudinary.com/dybdtehhs/image/upload/w_900,f_auto,q_auto/v1/zapatillasmay/hero-zapato 900w"
-        sizes="(max-width: 767px) 480px, 900px"
+        src="images/360/_DSC0846-Editar.webp"
         fetchpriority="high" width="900" height="900"
-        onerror="this.parentElement.parentElement.style.display='none'"
         alt="Calzado de moda para dama — Zapatillas May León Guanajuato" draggable="false">
       <div class="hero-shoe-shadow"></div>
-
+      <div class="viewer-360-badge">Arrastra para girar 360° ↺</div>
     </div>
   </div>
 
   <div class="hero-content fade-in" id="hero-content">
     <p class="hero-eyebrow">Directo del fabricante · León, Guanajuato</p>
-    <h1 class="hero-title">Calzado de dama<br>desde <em style="color:#a56780;text-shadow:0 0 20px rgba(136, 77, 101, 0.6),0 2px 0px rgba(211, 183, 194, 0.8)">León, GTO</em></h1>
-    <p class="hero-subtitle">Tacones, sandalias y botas desde <strong style="color:#e8c4a0">$360 MXN</strong> · Directo del fabricante · Envío a todo México</p>
+    <h1 class="hero-title">Calzado de dama<br>desde <em class="leon-gto" style="color:#a56780;text-shadow:0 0 20px rgba(136, 77, 101, 0.6),0 2px 0px rgba(211, 183, 194, 0.8)">León, Gto</em></h1>
+    <p class="hero-subtitle">Tacones, sandalias y botas desde <strong style="color:#b5687a">$360 MXN</strong> · Directo del fabricante · Envío a todo México</p>
     <p class="hero-offer" style="display:inline-flex;align-items:center;gap:8px;margin:14px 0 4px;padding:9px 16px;background:linear-gradient(135deg,rgba(156,90,82,0.95),rgba(166,121,95,0.92));border:1px solid rgba(232,196,160,0.55);border-radius:100px;font-size:0.82rem;font-weight:600;color:#fff;box-shadow:0 4px 14px rgba(120,70,60,0.25)">🏷️ Compra <strong>3 pares</strong> y paga <strong>precio de mayoreo</strong> + <strong style="color:#7CFFB2">envío GRATIS</strong></p>
     <div class="hero-cta">
       <button class="btn-primary" style="padding:16px 44px;font-size:0.88rem" onclick="scrollToProductos()">Ver novedades esta semana →</button>
@@ -6425,8 +6715,11 @@ function zmRechazarCookies() {
     // ── HERO BG: revelar + parallax scroll ──────────────────────────
     var heroBg = document.getElementById('hero-bg');
     if (heroBg) {
-      // Revelar fondo (reemplaza .hero.loaded que se quitó)
-      gsap.to(heroBg, { opacity: 1, duration: 0.6, ease: 'power1.out' });
+      // Ken Burns: zoom lento (CSS maneja el fade-in con transition)
+      gsap.fromTo(heroBg,
+        { scale: 1.07 },
+        { scale: 1, duration: 9, ease: 'power2.out' }
+      );
       // Parallax suave al hacer scroll
       gsap.to(heroBg, {
         yPercent: 12,
@@ -6443,14 +6736,19 @@ function zmRechazarCookies() {
     function runHeroEntry() {
       gsap.fromTo(
         '.hero-eyebrow, .hero-title, .hero-subtitle, .hero-offer, .hero-cta',
-        { opacity: 0, y: 50 },
-        { opacity: 1, y: 0, duration: 1.1, ease: 'power3.out', stagger: 0.18 }
+        { opacity: 0, y: 36, filter: 'blur(6px)' },
+        { opacity: 1, y: 0, filter: 'blur(0px)', duration: 1.0, ease: 'power3.out', stagger: 0.16 }
       );
       gsap.fromTo(
         '#hero-shoe-wrap',
-        { opacity: 0, x: 80, y: 24 },
-        { opacity: 1, x: 0, y: 0, duration: 1.5, ease: 'power3.out', delay: 0.15 }
+        { opacity: 0, y: 30 },
+        { opacity: 1, y: 0, duration: 1.3, ease: 'power3.out', delay: 0.2 }
       );
+      // Subrayado de "León, GTO" dibuja la línea cuando el título ya aterrizó
+      setTimeout(function() {
+        var leGTO = document.querySelector('.hero-title .leon-gto');
+        if (leGTO) leGTO.classList.add('line-drawn');
+      }, 1100);
     }
     // Si el splash ya se fue (navegación rápida / caché), corre de inmediato
     if (!document.getElementById('zm-splash')) {
@@ -6648,83 +6946,124 @@ function zmRechazarCookies() {
 })();
 </script>
 
-<!-- ── HERO 3D PARALLAX + SPOTLIGHT ─────────────────────────────── -->
+<!-- ── HERO 360° ROTATOR ─────────────────────────────── -->
 <script>
 ;(function() {
-  const section  = document.getElementById('hero-section')
-  const bg       = document.getElementById('hero-bg')
-  const content  = document.getElementById('hero-content')
-  const spotlight = document.getElementById('hero-spotlight')
-  if (!section || !bg) return
-
-  // Solo desktop para el parallax
-  const isDesktop = window.innerWidth >= 768
-
-  let tX = 0, tY = 0   // target
-  let aX = 0, aY = 0   // actual (smooth)
-  let rafId
-
-  if (isDesktop) {
-    section.addEventListener('mousemove', function(e) {
-      const r = section.getBoundingClientRect()
-      tX = ((e.clientX - r.left) / r.width  - 0.5) * 2   // -1 → 1
-      tY = ((e.clientY - r.top)  / r.height - 0.5) * 2
-
-      // Spotlight sigue al cursor
-      if (spotlight) {
-        spotlight.style.background =
-          `radial-gradient(circle 380px at ${e.clientX - r.left}px ${e.clientY - r.top}px,
-           rgba(255,225,200,0.13) 0%, transparent 70%)`
-      }
-    })
+  const container = document.getElementById('hero-shoe-scroll');
+  const imgElement = document.getElementById('hero-shoe-img');
+  const btnZoom = document.getElementById('btn-zoom');
+  if (!container || !imgElement) return;
+
+  const totalFrames = 17;
+  const startNum = 846;
+  const images = [];
+  let currentFrame = 0;
+  let isDragging = false;
+  let startX = 0;
+  
+  // Sensibilidad: píxeles de arrastre por cambio de frame
+  const pixelsPerFrame = 12;
 
-    section.addEventListener('mouseleave', function() {
-      tX = 0; tY = 0
-      if (spotlight) spotlight.style.background =
-        'radial-gradient(circle 300px at 50% 50%, rgba(255,230,210,0.10) 0%, transparent 70%)'
-    })
+  // Construir las URLs de los 17 frames (con cero a la izquierda)
+  for (let i = 0; i < totalFrames; i++) {
+    images.push(`images/360/_DSC${String(startNum + i).padStart(4, '0')}-Editar.webp`);
+  }
 
-    function tick() {
-      aX += (tX - aX) * 0.055
-      aY += (tY - aY) * 0.055
+  // Precarga diferida: el frame 1 ya está en el HTML (LCP). El resto se carga
+  // cuando el navegador está libre, para no competir con el primer pintado.
+  function preloadFrames() { images.forEach(u => { const im = new Image(); im.src = u; }); }
+  if ('requestIdleCallback' in window) requestIdleCallback(preloadFrames, { timeout: 2500 });
+  else window.addEventListener('load', preloadFrames);
 
-      if (Math.abs(aX) > 0.001 || Math.abs(aY) > 0.001) {
-        // Fondo: se mueve al revés (capa lejana) — efecto profundidad
-        bg.style.transform =
-          `translateX(${aX * -22}px) translateY(${aY * -12}px) scale(1.06)`
+  const badge = container.querySelector('.viewer-360-badge');
 
-        // Texto: se mueve con el cursor (capa cercana)
-        if (content) content.style.transform =
-          `translateX(${aX * 9}px) translateY(${aY * 5}px)`
-      }
-      rafId = requestAnimationFrame(tick)
+  // Desactivar comportamientos de arrastre y selección del navegador (Evita el color azul)
+  container.addEventListener('dragstart', (e) => e.preventDefault());
+  container.addEventListener('selectstart', (e) => e.preventDefault());
+  imgElement.addEventListener('dragstart', (e) => e.preventDefault());
+
+  // Controladores de eventos para arrastrar y rotar
+  function startDragging(e) {
+    isDragging = true;
+    startX = e.clientX || e.touches[0].clientX;
+    if (badge && badge.style.opacity !== '0') {
+      badge.style.transition = 'opacity 0.4s ease';
+      badge.style.opacity = '0';
     }
-    tick()
   }
 
-  // 3D tilt en la zapatilla
-  const shoeWrap = document.getElementById('hero-shoe-wrap')
-  const shoeImg  = document.getElementById('hero-shoe-img')
-  if (shoeWrap && shoeImg && isDesktop) {
-    section.addEventListener('mousemove', function(e) {
-      const r = section.getBoundingClientRect()
-      const x = (e.clientX - r.left) / r.width  - 0.5
-      const y = (e.clientY - r.top)  / r.height - 0.5
-      shoeImg.style.transform =
-        `perspective(800px) rotateY(${x * 18}deg) rotateX(${-y * 10}deg) scale(1.04) translateY(${y * -8}px)`
-    })
-    section.addEventListener('mouseleave', function() {
-      shoeImg.style.transform = 'perspective(800px) rotateY(0deg) rotateX(0deg) scale(1) translateY(0)'
-    })
+  function drag(e) {
+    if (!isDragging) return;
+    
+    const clientX = e.clientX || (e.touches ? e.touches[0].clientX : startX);
+    const deltaX = clientX - startX;
+    
+    const frameOffset = Math.floor(deltaX / pixelsPerFrame);
+    
+    let nextFrame = (currentFrame - frameOffset) % totalFrames;
+    if (nextFrame < 0) nextFrame += totalFrames;
+    
+    imgElement.src = images[nextFrame];
   }
 
-  // ── PARALLAX SCROLL ──────────────────────────────────────────────
-  const shoeScrollEl = document.getElementById('hero-shoe-scroll')
-  const sectionH = section.offsetHeight
-  let scrollTick = false
+  function stopDragging(e) {
+    if (!isDragging) return;
+    isDragging = false;
 
-  // Sin parallax en scroll — solo el mouse es interactivo
-})()
+    const clientX = e.clientX || (e.changedTouches ? e.changedTouches[0].clientX : startX);
+    const deltaX = clientX - startX;
+    const frameOffset = Math.floor(deltaX / pixelsPerFrame);
+    
+    currentFrame = (currentFrame - frameOffset) % totalFrames;
+    if (currentFrame < 0) currentFrame += totalFrames;
+  }
+
+  // Eventos de Mouse (Desktop)
+  container.addEventListener('mousedown', startDragging);
+  window.addEventListener('mousemove', drag);
+  window.addEventListener('mouseup', stopDragging);
+
+  // Eventos de Toque (Móvil)
+  container.addEventListener('touchstart', startDragging, { passive: true });
+  window.addEventListener('touchmove', drag, { passive: true });
+  window.addEventListener('touchend', stopDragging);
+
+  // Lógica de Zoom (Lupa y Doble Clic)
+  function toggleZoom(e) {
+    if (e) {
+      e.preventDefault();
+      e.stopPropagation();
+    }
+    imgElement.classList.toggle('zoomed');
+    if (imgElement.classList.contains('zoomed')) {
+      if (btnZoom) {
+        btnZoom.innerHTML = '➖';
+        btnZoom.style.background = '#C8967A';
+        btnZoom.style.color = '#ffffff';
+      }
+    } else {
+      if (btnZoom) {
+        btnZoom.innerHTML = '🔍';
+        btnZoom.style.background = '#FBF7F3';
+        btnZoom.style.color = '#2A1A0E';
+      }
+    }
+  }
+
+  if (btnZoom) btnZoom.addEventListener('click', toggleZoom);
+  imgElement.addEventListener('dblclick', toggleZoom);
+
+  // Giro de bienvenida (respeta prefers-reduced-motion)
+  if (!window.matchMedia || !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
+    let welcomeSpins = 0;
+    const welcomeInterval = setInterval(() => {
+      currentFrame = (currentFrame + 1) % totalFrames;
+      imgElement.src = images[currentFrame];
+      welcomeSpins++;
+      if (welcomeSpins >= 8) clearInterval(welcomeInterval); // Medio giro
+    }, 95);
+  }
+})();
 </script>
 
 <!-- ═══ BOTTOM NAV (solo móvil) ═══ -->
@@ -6867,5 +7206,218 @@ function zmRechazarCookies() {
     .product-badge { animation: none !important; }
   }
 </style>
+
+<!-- ═══ HERO — fixes globales (aplican en todos los tamaños) ═══ -->
+<style>
+/* Canvas de partículas inyectado por JS — ocultar siempre */
+#hero-section > canvas {
+  display: none !important;
+}
+/* GSAP aplica translateY(16px) en hero-content como estado inicial en todos los tamaños */
+#hero-content {
+  transform: none !important;
+}
+</style>
+
+<!-- ═══ HERO MÓVIL — layout definitivo (último bloque, gana todo) ═══ -->
+<style>
+@media (max-width: 768px) {
+
+  /* HERO: columna simple, sin padding, margin correcto al header real */
+  #hero-section {
+    display: flex !important;
+    flex-direction: column !important;
+    align-items: stretch !important;
+    justify-content: flex-start !important;
+    padding: 0 !important;
+    margin-top: 145px !important;
+    height: auto !important;
+    min-height: auto !important;
+    overflow: visible !important;
+  }
+
+  /* Ocultar fondo flotante + elementos no deseados en móvil */
+  #hero-bg,
+  .hero-spotlight,
+  .hero-particles,
+  #btn-zoom,
+  .viewer-360-badge,
+  .hero-eyebrow,
+  .hero-subtitle {
+    display: none !important;
+  }
+
+  /* ── SECCIÓN 1: Visor 360 — blanco, full width, flush arriba ── */
+  #hero-shoe-scroll {
+    order: 1 !important;
+    position: relative !important;
+    top: auto !important; bottom: auto !important;
+    left: auto !important; right: auto !important;
+    width: 100% !important;
+    height: 48vh !important;
+    min-height: 260px !important;
+    margin: 0 !important;
+    padding: 0 !important;
+    background: #ffffff !important;
+    display: flex !important;
+    align-items: center !important;
+    justify-content: center !important;
+    overflow: hidden !important;
+    z-index: 2 !important;
+    pointer-events: auto !important;
+    cursor: grab;
+    touch-action: pan-y;
+    /* Bloquear parallax de GSAP en móvil */
+    transform: none !important;
+  }
+
+  /* Badge "360°" dentro del visor, esquina superior derecha */
+  #hero-shoe-scroll::after {
+    content: '360°';
+    position: absolute;
+    top: 14px;
+    right: 14px;
+    font-family: var(--font-sans);
+    font-size: 0.6rem;
+    font-weight: 600;
+    letter-spacing: 0.07em;
+    color: rgba(165,103,128,0.9);
+    background: rgba(255,255,255,0.88);
+    border: 1px solid rgba(165,103,128,0.2);
+    padding: 3px 8px;
+    border-radius: 20px;
+    z-index: 3;
+    pointer-events: none;
+  }
+
+  /* ── SECCIÓN 2: Foto lifestyle + texto ── */
+  .hero-content {
+    transform: none !important;
+    order: 2 !important;
+    position: relative !important;
+    left: auto !important; right: auto !important;
+    width: 100% !important;
+    max-width: 100% !important;
+    padding: 0 0 72px !important;
+    display: flex !important;
+    flex-direction: column !important;
+    align-items: center !important;
+    justify-content: flex-end !important;
+    min-height: 56vw !important;
+    background-image: url('https://res.cloudinary.com/dybdtehhs/image/upload/w_700,f_auto,q_auto/v1777668328/ChatGPT_Image_1_may_2026_14_43_58_kkgzs9.png') !important;
+    background-size: cover !important;
+    background-position: center 60% !important;
+    text-align: center !important;
+    z-index: 1 !important;
+  }
+
+  /* Scrim: crema sólido abajo → transparente arriba */
+  .hero-content::before {
+    content: '' !important;
+    position: absolute !important;
+    inset: 0 !important;
+    background: linear-gradient(
+      to top,
+      rgba(251,247,243,0.97) 0%,
+      rgba(251,247,243,0.80) 38%,
+      rgba(251,247,243,0.20) 65%,
+      rgba(251,247,243,0.00) 100%
+    ) !important;
+    z-index: 0 !important;
+    pointer-events: none;
+  }
+
+  .hero-content > * {
+    position: relative !important;
+    z-index: 1 !important;
+  }
+
+  /* ── TÍTULO ── */
+  .hero-title {
+    font-size: 2.6rem !important;
+    line-height: 1.06 !important;
+    letter-spacing: -0.02em !important;
+    color: var(--black) !important;
+    text-shadow: none !important;
+    margin: 20px 24px 8px !important;
+    padding: 0 !important;
+  }
+  .hero-title .leon-gto {
+    text-shadow: none !important;
+  }
+  .hero-title .leon-gto::after {
+    display: none !important;
+  }
+
+  /* ── OFFER PILL — centrar y controlar wrapping ── */
+  .hero-offer {
+    align-self: center !important;
+    max-width: calc(100% - 32px) !important;
+    margin: 6px auto 10px !important;
+    flex-wrap: wrap !important;
+    justify-content: center !important;
+    gap: 3px 5px !important;
+    font-size: 0.72rem !important;
+    padding: 7px 14px !important;
+    text-align: center !important;
+  }
+  .hero-offer strong {
+    white-space: nowrap !important;
+  }
+
+  /* ── BOTONES CHICOS ── */
+  .hero-cta {
+    flex-direction: row !important;
+    justify-content: center !important;
+    gap: 10px !important;
+    width: 100% !important;
+    padding: 0 20px 24px !important;
+    margin-top: 4px !important;
+  }
+  .hero-cta .btn-primary,
+  .hero-cta .btn-outline {
+    flex: 1 !important;
+    max-width: 148px !important;
+    padding: 10px 14px !important;
+    font-size: 0.72rem !important;
+    justify-content: center !important;
+    width: auto !important;
+  }
+  .btn-outline {
+    border-color: var(--black) !important;
+    color: var(--black) !important;
+  }
+
+  /* Zapato: imagen más grande dentro del visor */
+  .hero-shoe-wrap {
+    opacity: 1 !important;
+    transform: none !important;
+  }
+  .hero-shoe-wrap img {
+    max-height: 85% !important;
+    width: auto !important;
+    object-fit: contain !important;
+  }
+}
+
+/* ── DESKTOP: hero compacto + composición balanceada ── */
+@media (min-width: 769px) {
+  /* 60vh → elimina el espacio vacío sin romper el layout del zapato absoluto */
+  .hero {
+    height: 60vh !important;
+    min-height: 500px !important;
+    max-height: 680px !important;
+  }
+  /* Centrado vertical — igual espacio arriba y abajo del bloque de texto */
+  .hero-content {
+    justify-content: center !important;
+    padding-top: 0 !important;
+  }
+  /* Zapato centrado verticalmente en su columna derecha */
+  .hero-shoe-wrap {
+    align-items: center !important;
+  }
+}
+</style>
 </body>
 </html>
\ No newline at end of file
